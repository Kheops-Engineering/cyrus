{
  "private": true,
  "scripts": {
    "clean": "lerna clean",
    "bootstrap": "lerna bootstrap",
    "test": "CI=true lerna run test --stream",
    "start": "lerna run --parallel start",
    "prepare": "rimraf web/dist && rimraf common/dist && lerna run prepare",
    "build": "npm run prepare && lerna run --parallel build"
  },
  "devDependencies": {
    "@types/node": "^14.0.14",
    "lerna": "^3.22.1",
    "rimraf": "^3.0.2",
    "typescript": "^3.9.5"
  },
  "workspaces": [
    "web",
    "ui-common",
    "ui-components"
  ]
}
